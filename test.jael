x = 'a';
g = /ok//and find/;
def fun(a,b):
	if a@outer>0: 
		s = a;
	elif b>a:
		s = b;
	else:
		s = a+b;
	;
	for i in a:
		s += i;
	else: 
		fun(2,3);
	;
	case x:
		in 3: puts:x; 
			continue; --- break by default
		in 4: puts:x;
		else: pass;
	;
;

while x<3: 
	x+=1; 
else:
	x+=1;
; --- end while

:: --- do=while
	x += 1;
while: x<10;

puts: "test", 5 ;
array:int[] = {2,3,4,5};
qq = {"a", "b", "c"}; ---array
xb = <int>[2]; 
seta = <2,3,5>;
lam = |a,b|{a+b};
class A:
	stat = /[0-9]+/;
	count = 0;

	def meth(a):
		.a = a;
	;

	def cmeth@class():
		:local = 1;
		count@final = 2;
		.count@super = 1;
		x[3] = 2;
		g().m = 1;
		g() = 1; --- bad
	;
;
